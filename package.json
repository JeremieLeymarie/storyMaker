{
  "name": "storymaker",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "cli": "ts-node-dev --respawn --transpile-only index.ts",
    "dev": "ts-node-dev --respawn --transpile-only index.ts",
    "test": "ts-node-dev --respawn --transpile-only test.ts",
    "serve": "ts-node-dev --respawn --transpile-only server.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.9.0",
    "cli-color": "^2.0.1",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "ts-node-dev": "^1.1.8"
  },
  "devDependencies": {
    "@types/cli-color": "^2.0.2",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.21"
  }
}
